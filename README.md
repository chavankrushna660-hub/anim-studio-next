# Anim Studio Pro

Clone this git repository - https://github.com/chavankrushna660-hub/Animstudio_10.git Convert this entire animation app into real software

make it real, refine the UI, don't keep multicolored UI like buttons are some in background of button some ae yellow and green and mixed colors that looks too old, the software should clean and feel real, like you ever seen the Adobe softwares just like they looks professional and clean. and works faster.

and the strict warning to you is keep all the features as it is, means they should work as it is how they working now,

make the app as fast as you can, and the app should handle bigger projects, it should handle 100k object and Drawings together at ones make the app such faster, it should handle 100k objects and drawings at ones it still very fast and work very fast. make the app real like if any features are missing or any add text or multiple shapes tool Into the app. Make the app very ultra stable and faster when it handling 100k object at ones. Create real bigger workspace for handling and managing multiple objects and Drawings, add database if you can. but the strict rule is everything should be free forever keep ads running. refine UI according to you. Remember the strict  warning keep all the tools and features as it is, without changing or modifying it's internal logic to working it. and all the app as it is just change the UI and make app faster and you can add some tools and features that are essential for animation, but strictly keep the features and tools and their internal workings as it is. Don't change it just add more. Convert it to real softwares, you can convert this app into desktop app the animation will save on users  hardware on user system. Do anything you want. Keep the app logo as it is.  remove yellow and blue and green all the color add only one theme one code, remove transparent colors or transparent backgrouunds only add real one color remove the button pyhton codebase from the app, and make logos position fixed when draggin or swapping the top baar logo should not drag logo should only stays at its fixed position. 

light gray background should come for active tools, and remove the background color from the logo, keep it without background and strictly dont change anything from the app. and are you mad, dont skip this space in tool baar, when expanding the toolbar then it should come in right panel the and left panel all the color picker should be full width of the right panal all the element should in straight direction vertically any of the elements should not go out o the straight panel. can not overlap or go out of boundry. and the very strict warning to you is strictly dont replace or overide any feature in backend only refine the UI



are you mad keep the logo color to as it is as before, and remove the background color of the logo mans the entire span background color only not the logo itself, and make the color picker bigger it should get full width of right panel, all the color pickers of this app, and the top baar should not dragged behind from the logo it look old, if it dragged behinf from the logo it should not visible the is dragging the top baar tool from behind the logo. and make canvas size by default full the canvas area should not black or blank anyway.

are tumhari maa ka bhosda tumhari logo ke color ko pehle jaisa that vaisa karo, pehle red tha, or jo black background lagaya hai logo ke piche use hataya rand ke sale. are tumhari maa ka bhosda tumhari logo ke color ko pehle jaisa that vaisa karo, pehle red tha, or jo black background lagaya hai logo ke piche use hataya rand ke sale.



Keep the logo color as it is, and logo should not dragged or Swapable, means it's possible should fixed and other top bar tools should not visible while dragging it, should not go from backward of the logo, add one color to app, like drak to entire theme, like, panels buttons, and all. left right panels components, color picker should get full width of the panels. Create font bold and like text white and background color dark grey or black. and logo svg should as it is.



Real adobe like theme to app buttons and all the elements and components. keep the logo as it is fixed position. Remove app loading animation remove flash screen don't add it to app, and convert keep this theme but convert it very small slightly to blue tone, not text only text should be white, and bold. And fix some bugs dark and light theme toggle should available.

This project was built with [Lovable](https://lovable.dev).

**Live app**: https://anim-studio-next.lovable.app

## Build with Lovable

Continue developing this project in the [Lovable editor](https://lovable.dev/projects/b4a4345c-37de-43e5-9ed3-12b7b4a1bfd4).

- **Ship faster**: describe what you want to build and Lovable handles the code.
- **Stay in sync**: every change made in Lovable is committed straight to this repository.
- **Full ownership**: this code is yours. Push to `main` on GitHub and your changes sync back into Lovable, ready for your next prompt.

## Development

Prefer working locally? You need Node.js and npm — [install with nvm](https://github.com/nvm-sh/nvm#installing-and-updating).

```sh
git clone <this-repository-url>
cd <repository-name>
npm i
npm run dev
```
